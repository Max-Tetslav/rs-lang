import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';
import App from './App';

const root: HTMLElement = document.getElementById('app-root') as HTMLElement;

ReactDOM.render(<App />, root);